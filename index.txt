<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>THE FOUNDRY CLUB by Fierro Records</title>
  <style>
    /* RESET BÁSICO */
    * { margin:0; padding:0; box-sizing:border-box; }

    /* FONDO OSCURO Y PARTÍCULAS */
    body {
      font-family: 'Arial', sans-serif;
      color: #fff;
      background: #0d0d0d;
      overflow-x: hidden;
    }

    .background {
      position: fixed;
      width: 100%;
      height: 100%;
      background: url('https://via.placeholder.com/1200x800') no-repeat center center/cover;
      filter: brightness(0.3);
      z-index: -2;
    }

    /* PARTICULAS SUTILES */
    .particles {
      position: fixed;
      width: 100%;
      height: 100%;
      z-index: -1;
      background: radial-gradient(circle, rgba(255,255,255,0.05) 1px, transparent 1px);
      background-size: 50px 50px;
      animation: moveParticles 10s linear infinite;
    }

    @keyframes moveParticles {
      0% {background-position: 0 0;}
      100% {background-position: 50px 50px;}
    }

    /* CONTENEDOR CENTRAL */
    .container {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      text-align: center;
      padding: 20px;
    }

    h1 {
      font-size: 3rem;
      margin-bottom: 20px;
      animation: fadeIn 2s ease forwards;
      opacity: 0;
    }

    h2 {
      font-size: 1.5rem;
      margin-bottom: 10px;
      animation: fadeIn 2s ease 1s forwards;
      opacity: 0;
    }

    p {
      font-size: 1.2rem;
      margin-bottom: 20px;
      animation: fadeIn 2s ease 2s forwards;
      opacity: 0;
    }

    .button {
      padding: 15px 30px;
      background: #ff0055;
      color: #fff;
      text-decoration: none;
      font-weight: bold;
      border-radius: 5px;
      transition: 0.3s;
      animation: fadeIn 2s ease 3s forwards;
      opacity: 0;
    }

    .button:hover {
      background: #ff3366;
    }

    /* ANIMACIONES */
    @keyframes fadeIn {
      to { opacity: 1; }
    }

    /* RESPONSIVE */
    @media(max-width:768px) {
      h1 { font-size: 2rem; }
      h2 { font-size: 1.2rem; }
      p { font-size: 1rem; }
    }
  </style>
</head>
<body>
  <div class="background"></div>
  <div class="particles"></div>

  <div class="container">
    <h1>THE FOUNDRY CLUB</h1>
    <h2>by Fierro Records</h2>
    <p>📅 Viernes 24 de Octubre | 23:00 - 05:00</p>
    <p>📍 San Andrés, San Martín, Buenos Aires</p>
    <p>🎵 Line up: F.I.E.R.A. (live) - Diego Amaro - ENEB</p>

    <!-- BOTÓN MERCADO PAGO -->
    <a href="TU_LINK_MERCADOPAGO" target="_blank" class="button">Comprar Entrada</a>

    <!-- FLYER / IMAGEN -->
    <div style="margin-top:30px;">
      <img src="https://via.placeholder.com/600x800" alt="Flyer del Evento" style="max-width:90%; border-radius:10px;">
    </div>
  </div>
</body>
</html>
